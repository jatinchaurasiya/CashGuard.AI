# Strands Agents SDK - Agent orchestration framework
strands-agents>=0.1.0

# OpenAI client - Required by Strands Agents to connect to OpenAI-compatible APIs like OpenRouter
openai>=1.0.0

# Environment variables management (loads .env files)
python-dotenv>=1.0.0

# Data modeling and validation
pydantic>=2.0.0
